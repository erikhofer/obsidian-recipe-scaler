// Minimal stub so vitest can import canvas-resolver.ts without a real Obsidian
// runtime.  All values are empty — tests pass the real implementations via
// constructor injection or cast them away with `as any`.
export class App {}
export class TFile {}
export class Plugin {}
export const EventRef = {};
